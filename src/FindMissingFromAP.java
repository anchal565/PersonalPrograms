public class FindMissingFromAP {
    public static void main(String[] args) {
        int[] arr = {2,4,8,10};
        //find diff
        //diff = (final - start)/n;
        //find expected mid  = a+(mid * diff)
        //find actual mid
        //while(start != end)
        //return expected arr at index mid;
        //if(arr[mid] == expected)
        //missing lie in right part
        //start = mid+1
        //else
        //check in left part
        //end = mid;

    }
}
